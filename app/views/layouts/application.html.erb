<!DOCTYPE html>
<html>
  <head>
    <title>gmaps</title>
    <meta name="viewport" content="width=device-width">
    <script src="https://maps.google.com/maps/api/js?v3&key=AIzaSyBAWgF_RcfttdtN6-Q4RX9OsjiQ2hD5n98&callback=initMap" async="async" defer="defer"></script>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <link href="https://use.fontawesome.com/releases/v5.0.6/css/all.css" rel="stylesheet">
    <%= csrf_meta_tags %>
  </head>
  <body>
    <%= render 'layouts/header' %>
    <div class="row">
      <div class="box col-xs-12 col-md-8">
        <%= yield %>
      </div>
      <div class="box col-xs-12 col-md-4">
        <%= render 'layouts/aside' %>
      </div>
    </div>
  </div>
  <footer class="footer">
    <div class="container">
      <p class="text-muted">Place sticky footer content here.</p>
    </div>
  </footer>
  <% unless flash.empty? %>
    <script type="text/javascript">
      <% flash.each do |f| %>
        <% type = f[0].to_s.gsub('danger','error').gsub('alert','error').gsub('notice','info') %>
          toastr['<%= type %>']('<%= f[1] %>');
      <% end %>
    </script>
  <% end %>
  </body>
</html>
